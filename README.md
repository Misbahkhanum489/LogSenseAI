# LogSenseAI
LogSense AI is a hybrid log classification system that combines Regex, BERT, and LLMs to automatically analyze and categorize technical logs. It includes a FastAPI backend and interactive web dashboard for CSV upload, classification, filtering, visualization, and downloading results.
